{
	"name": "@apps/backend",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun run --hot index.ts",
		"build": "bun build index.ts --outdir dist"
	},
	"dependencies": {
		"hono": "^4.11.7"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
