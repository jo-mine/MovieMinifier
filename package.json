{
	"workspaces": [
		"packages/*",
		"packages/ifs/*"
	],
	"scripts": {
		"dev": "bun run --workspaces dev",
		"dev:backend": "bun run --filter ./packages/backend dev",
		"dev:frontend": "bun run --filter ./packages/frontend dev",
		"build": "bun run --workspaces build"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.13",
		"@types/bun": "latest"
	}
}
